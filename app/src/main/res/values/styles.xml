<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/headerColor</item>
        <item name="android:textColor">@color/fontColor1</item>
        <item name="android:titleTextColor">@color/fontColor1</item>
        <item name="android:color">@color/fontColor1</item>
    </style>


</resources>
