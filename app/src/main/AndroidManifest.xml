<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fruticontrol">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- TODO: retirar usesCleartextTraffic para producción!!! -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"><!-- TODO: retirar usesCleartextTraffic para producción!!! --
        <activity android:name=".NuevaGranjaActivity"></activity>
        <activity android:name=".ListaGranjasActivity" />
        <activity android:name=".CambiarContrasenaActivity" />
        <activity android:name=".PerfilActivity" /> 
        <activity android:name=".AccionesActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>